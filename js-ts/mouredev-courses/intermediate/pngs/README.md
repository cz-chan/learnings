# pngs — Imágenes de las lecciones de DOM

Carpeta de recursos, no de código. Aquí van las imágenes que cargan las páginas del tema **06 — DOM** (`../06-DOM.example.html`, `../06-DOM.tasklist.html`).

Contenido actual:

- `skull.png`

Si añado una imagen nueva a una lección del DOM, va aquí y se referencia con una ruta relativa desde el HTML (`pngs/nombre.png`).
