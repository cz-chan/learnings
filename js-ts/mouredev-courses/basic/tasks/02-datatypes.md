1. Escribe un comentario en una lÃ­nea.
   // hola mundo
2. Escribe un comentario en varias lÃ­neas.
   /_*
   hola
   mundo
   *_/
3. Declara variables con valores asociados a todos los datos de tipo **primitivos**.

- let string = "hola"
- let number = 9
- let bool = true
- let mysym = Symbol("mysym")
- let bigInt = BigInt(987123897123987123987123987123987123987123987123987123987123)
- let bigInt2 = 234567890987654321234567898765432345678987654321234567898765432123456789n

4. Imprime por consola el valor de todas las variables.

- console.log(string)
- console.log(number)
- console.log(bool)
- console.log(mysym)
- console.log(bigInt)
- console.log(bigInt2)

5. Imprime por consola el tipo de todas las variables.

- console.log(typeof string) // string
- console.log(typeof number) // number
- console.log(typeof bool) // boolean
- console.log(typeof mysym) // symbol
- console.log(typeof bigInt) // bigint
- console.log(typeof bigInt2) // bigint

6. A continuación, modifica los valores de las variables por otros del mismo tipo.

- string = "hola de nuevo" o string = 7 -> porque al no ser typescript puedo cambiar los tipos de datos ya que los valores sí puedo cambiarlos por ser let y no const

7. A continuación, modifica los valores de las variables por otros de distinto tipo.

- bigInt2 = "hola"

8. Declara constantes con valores asociados a todos los tipos de datos primitivos.

- const unString = "hola mundo"
- const unNum = 7
- const boll = true
  ...

9. A continuación, modifica los valores de las constantes.

- no se puede

10. Comenta las líneas que produzcan algún tipo de error al ejecutarse.

- ninguna pero sería el cambio de valores de las constantes
